import { SELECT_VALUE } from '@sh/text-constants';
import type { DropdownOptions } from '@sh/types';

export const SELECT2_EMPTY_DATA = [] as DropdownOptions;

export const SELECT2_DEFAULT_CONFIG = {
  theme: 'dark-adminlte',
  placeholder: SELECT_VALUE,
};
