import * as NodeRed from 'node-red';

import type { {{startCase name}}Backend, {{startCase name}}BackendProps } from './types';

export default (RED: NodeRed.Red) => {
  function {{startCase name}}Constructor(this: {{startCase name}}Backend, props: {{startCase name}}BackendProps) {
    RED.nodes.createNode(this, props);

    this.on('input', (msg, send, done) => {
      send(msg);
      done();
    });
  }

  RED.nodes.registerType('{{name}}', {{startCase name}}Constructor);
};
